@{
    ViewData["Title"] = "Home Page";
}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="#">YOUR DASHBOARD</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <p class="nav-link">@Html.ActionLink("GetAllTransaction", "GetAllTransaction", "AccountProfile", null, new { @class = "btn btn-primary" })</p>
                </li>
                <li class="nav-item">
                    <p class="nav-link">@Html.ActionLink("Create New Paypee", "New_Paypee", "AccountProfile", null, new { @class = "btn btn-primary" })</p>
                </li>
                <li class="nav-item">
                    <p class="nav-link">@Html.ActionLink("New Transaction", "NewTransaction", "AccountProfile", null, new { @class = "btn btn-primary" })</p>
                </li>
                <li class="nav-item">
                    <p class="nav-link">@Html.ActionLink("Logout", "Logout", "User", null, new { @class = "btn btn-primary" })</p>
                </li>

            </ul>
        </div>
    </div>
</nav>